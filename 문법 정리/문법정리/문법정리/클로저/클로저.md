# 클로저
 **이름 없이 사용하는 함수** = **필요할 때 바로 쓰는 임시 함수**
 
 *정의*

 ```swift
 { (매개변수 목록) -> 반환타입 in
    실행 코드
}
 ```swift
let asdf = { (name: String) in
    print("안녕 \(name)")
}

asdf("시우")
 ```

## 변수 상수 등으로 저장, 전달인자로 전달이 가능