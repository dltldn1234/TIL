# DTO
**Data Transform Object**
```swift
DTO는 원시값이다
```
![alt text](<스크린샷 2026-05-06 오후 5.01.45.png>)
이게 DTO의 예시

여기에 key랑 value이 쓰인다 근데 swift에서 유연하게 쓰일수는 없다

근데 이걸 swift에서 유연하게 쓰일수있도록 쓰는게  Domain Model

# Domain Model

**title_A : string 가 DTO에서 Domain Model로 넘어오면 title : string**









# DTO -> (맵핑) -> Domain Model