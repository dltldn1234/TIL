# 선언형
짧아

# 명령
길어어 1~100까지 하나하나 직접 다 일일이 입력하고 순서대로 시행하느라 
겁나게 복잡함


# 스유
## 장점
1. 선언형

2. 코드가 짧고 직관적 이다.

3. 개발 속도가 엄청 빠르다

```swift
Text("Hello")
.font(suit)
```

c언어의 print와는 다르다

## 단점
1. 디테일이 부족하다
* 

    * 위치선언 X 실질적인 좌표 값이 없다
        
        * vstack
        ```swfit
            Text("Hello")
            Text(" 시우 ")
            // 하면 세로로 나옴!
        ```
2. 스유로 구현이 안되는것들이 많음
* 애니메이션
* 위치
* 





```swift
struct Myview: view
에서는 로직 즉 계산을 넣으면 안된다

1. struct Myview: view
2.    count += 1
 상태변경이 되기때문에 무한반복이 된다
 1번에서 2번으로 넘어가고나서   2번에서 값이 바껴서 1번으로 
```



















































스트럭트 위에 바디가 쌓이고 그위에 브이스택이 쌓이고 헬로우를 쓴다 그러면
헬로우 말고 하이를 쓰면 그위에 위에 과정들이 다시 쌓이고 하이를 쓴다 이것들이 매우 비효율적 이다 이것이 ios의 단점 

