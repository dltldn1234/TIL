server -> service -> dto(model) -> view model -> view

* 뒤에 있는 데이터 흐름들은 서버라는 매개체에 종속되어있는 것 

server  에서 dto로 갈때 그 중간 과정이 서비스 임

**BaseUrl 은 서버의 주소**

baseurl을 바로 변수로 선언을 하면 터지지는 않는데 let으로 선언을 해야 바뀌지 않는다 (중요함!)

다른 파일에서 baseurl을 알면 안된다 (private으로 선언)


