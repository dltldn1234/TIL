```swift

//
//  PostService.swift
//

import Foundation

final class PostService {

    static let shared = PostService()

    private init() {}

    private let baseURL = "https://jsonplaceholder.typicode.com/posts"

    func fetchPosts() async throws -> [Post] {

        guard let url = URL(string: baseURL) else {
            throw NetworkError.invalidURL
        }

        let (data, response) =
        try await URLSession.shared.data(from: url)

        guard let httpResponse = response as? HTTPURLResponse else {
            throw NetworkError.invalidResponse
        } //다운 캐스팅

        guard 200...299 ~= httpResponse.statusCode else {
            throw NetworkError.serverError(httpResponse.statusCode)
        }

        let dto = try JSONDecoder().decode(
            [PostDTO].self,
            from: data
        )

        return dto.map {
            $0.toDomain()
        }
    }
}
``` 


## Final은 상속이 안된다는 것 이다 
* 파이널은 상속을 못하게하는것 
    * 상속이란? : 만약에 부모객체가 잇으면 자식객체가 그대로 부모의 특성을 이어받는 것 이다

### init은 호출을 당하면 바로 생성되는 객체다
```swift
Class 민준{
    init(){
        print("멍멍")
    }
}
```
이렇게 하면 다른 클래스에서 민준이라는 클래스를 호출하면 멍멍이 나온다


## 서비스는 상속을 못하게 한다 왜일까? 
* 자식 클래스가 부모 클래스에다가 덮어쓰기를 못하게 하려고 하기 때문임!

## Static
* 객체를 만들지 않아도 바로 쓸 수 있는 것
```swift
struct User {
    static let Name = "IT-DA"
}
```
라고 하면 호출을 할때
```swift
print(User.Name)
```
으로 호출을 하면 된다

--- 
## 싱글톤 패턴은 연속작용으로 계속 정보를 수정할때 마다 바뀐다
*예시*
```swift

var p1 = playerhp(-10)

var p2 = playerhp(-20)
```
이렇게 있으면 싱글톤 패턴이 hp = 100이면

싱글톤 패턴을 쓰면 p1에서 100 -10 을 하면 90이 된다 그리고 p2 에다가 또 적용을 하면 90 -20이 된다 = 70

일반적인경우는 각각 다른 객체로 봐서 100 -10 

100-20을 따로따로 하지만 싱글톤은 공유하기때문에 저렇게 바뀐다 

장점으로는 공유를 하기때문에 같은 메모리가 되지만 단점이 보안이 약하고 한개가 터지면 연쇄적으로 다 터진다 *장점이자 단점*

---


## 키워드란?
* 키워드는 스위프트가 미리 지정해둔 특별한 단어들임
```swift
 ex) let var 등등
```
    

##  async
* 비동기형식으로 오래 걸리는 작업을 기다리는 동안 앱이 안 멈추게 해주는 것 이다

*예시*
```swift
func fetchUser() async {
    let data = await 서버에서_데이터_가져오기()
    print(data)
}
```

## await은 비동기를 기다려주는것이다

데이터로 통신이 통신이 안들어와도 코드 파일은 유효성이 있어서 await는 데이터 통신이 될때 까지 기다려주는거다 

동기가 아닌 이유가 동기면 처음부터 다시 시작 하는데 비동기로해서 처음부터 실행을 안하고 멈춘곳부터 실행 하려고 쓴다


### throw 는 옵셔널이다 또 대체가 가능
* throw 는 에러를 발생 시키는 키워드 이다

*예시*
```swift
func login(password: String) throws {
    if password != "1234" {
        throw LoginError.wrongPassword
    }
}
```
이렇게 되어있다면 만약 비밀 번호가 틀리면

    throw LoginError.wrongPassword

이걸 내보내서 오류가 있음을 표시한다

### [int]는 무조건 배열 (1,2,3)

* 정수 배열

```swift
let age: Int = 17
```

## as는 타입 캐스팅 중 한개 이다

* 타입을 변환할때 쓰는 키워드(상속이나 프로토콜에서 많이씀)

```swift
class Animal {}
class Dog: Animal {}

let dog = Dog()
let animal = dog as Animal // 상속에서 Dog를 Animal 타입으로 변환후 저장
```


### try 에러가 날수 있다는 함수를 실행
*예시*
```swift
try login(password: "0219")
```


### shared는 싱글톤의 응용
* shared는 싱글톤에 접촉하기 위한 프로퍼티 이다

*예시*
```swift
URLSession.shared
```
shared는 공통 URLSession 객체이다

### ~= 는 재검토
* 패턴 매칭 할때 씀

*예시*
```swift
(1...10) ~= 7 // 1부터 10중 7이 있으면 True로 반환
```
결과값은 Bool 대수를 쓴다

## 서버 메소드 종류
```swift
Get - 조회
Path - 일부 수정
Post - 생성
Put - 전체 수정
Delete - 삭제
```









