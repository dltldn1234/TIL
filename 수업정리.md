```swift

//
//  PostService.swift
//

import Foundation

final class PostService {

    static let shared = PostService()

    private init() {}

    private let baseURL = "https://jsonplaceholder.typicode.com/posts"

    func fetchPosts() async throws -> [Post] {

        guard let url = URL(string: baseURL) else {
            throw NetworkError.invalidURL
        }

        let (data, response) =
        try await URLSession.shared.data(from: url)

        guard let httpResponse = response as? HTTPURLResponse else {
            throw NetworkError.invalidResponse
        } //다운 캐스팅

        guard 200...299 ~= httpResponse.statusCode else {
            throw NetworkError.serverError(httpResponse.statusCode)
        }

        let dto = try JSONDecoder().decode(
            [PostDTO].self,
            from: data
        )

        return dto.map {
            $0.toDomain()
        }
    }
}
``` 


## Final은 상속이 안된다는 것 이다 
* 파이널은 상속을 못하게하는거

## 만약에 부모객체가 잇으면 자식객체가 그대로 부모의 특성을 이어받는 것 이다

### init은 호출을 당하면 바로 생성되는 객체다
```swift
Class 민준{
    init(){
        print("멍멍")
    }
}
```
**이렇게 하면 다른 클래스에서 민준이라는 클래스를 호출하면 멍멍이 나온다 라는 느낌**


### 서비스는 상속을 하면안되는게 자식 클래스가 부모 클래스에다가 덮어쓰기르 못하게 하려고 

Static은 객체가 필요없다 라는 것 
* 따로 공부를 해야한다 스태틱


싱글톤 패턴은 연속작용으로 계속 바뀌게 된다

```swift

var p1 = playerhp(-10)

var p2 = playerhp(-20)
```
이렇게 있으면 싱글톤 패턴이 hp = 100이면
싱글톤 패턴을 쓰면 p1에서 100 -10 을 하면 90이 된다 그리고 p2 에다가 또 적용을 하면 90 -20이 된다 

일반적인경우는 각각 다른 객체로 봐서 100 -10 

100-20을 따로따로 하지만 싱글톤은 공유하기때문에 저렇게 바뀐다 

장점으로는 공유를 하기때문에 같은 메모리가 되지만 단점이 보안이 약하고 한개가 터지면 연쇄적으로 다 터진다 (나중에 정리 필요)


#### async가 비동기라는 뜻

throw 는 옵셔널이다 또 대체가 가능

int는 무조건 배열 (1,2,3)

개발에서 as는 바꾼다는거


try 에러가 있다는 뜻


shared는 싱글톤의 응용

서버 메소드 종류
```swift
Get - 조회
Path - 일부 수정
Post - 생성
```

### await은 비동기를 기다려주는것이다

데이터로 통신이 통신이 안들어와도 코드 파일은 유효성이 있어서 await는 데이터 통신이 될때 까지 기다려주는거다 

동기가 아닌 이유가 동기면 처음부터 다시 시작 하는데 비동기로해서 처음부터 실행을 안하고 멈춘곳부터 실행 하려고 쓴다


~= 는 재검토


