# 구조체 (값 타입)
* 데이터와 기능을 같이 묶는 하나의 타입 

**- 정의**
```swift
// struct 이름  {
//    /* 구현부 */
// }
```
**구조체 안에 메서드 사용도 가능!**

*구조체 인스턴스및 메서드 예시*
```swift
struct Sample {
    var mutableProperty: Int = 100     // 가변 프로퍼티
    let immutableProperty: Int = 100   // 불변 프로퍼티

    static var typeProperty: Int = 100 // 타입 프로퍼티

    // 인스턴스 메서드
    func instanceMethod() {
        print("instance method")
    }

    // 타입 메서드
    static func typeMethod() {
        print("type method")
    }
}
```

*인스턴스 메서드와 타입 메서드의 예*
```swift
 학생 구조체

struct Student {
    var name: String = "unknown"
    var `class`: String = "Swift" // class에 ' 쓰는이유 swift예약어라서 그냥 못 쓴다

    static func selfIntroduce() { // 타입 메서드
        print("학생타입입니다")
    }

    func selfIntroduce() { // 인스턴스 메서드
        print("저는 \(self.class)반 \(name)입니다")
    }
}
```


## 구조체 특징

1. 값 타입 : 복사하면 완전히 다른 값이 된다

2.  메모리 안전

* Swift는 구조체 사용을 많이 권장함

* Apple도

    *  모델
    * 데이터 구조

대부분 Struct 사용한다

Struct = 데이터 + 기능을 묶는  값 타입

### Struct의 장점
 1. 빠름 
 2. 메모리 안전  
 3. 코드 관리 쉬움

