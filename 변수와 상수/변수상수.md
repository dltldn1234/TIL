# 변수와 상수

**상수와 변수 선언**

- 상수 선언 키워드 let
- 변수 선언 키워드 var

## 상수의 선언 
```swift
let 이름: 타입 = 값
```  

## 변수의 선언
```swift
var 이름; 타입 = 값
```
## 값의 타입이 명확 하다면  타입은 생략 가능

```swift
let 이름 = 값
var 이름 = 값
```



## 차후에 변경
- 가능한 변수 var
- 불가능한 변수 let

